services:
  - type: web
    name: lyn-radar-relay
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node index.js
    healthCheckPath: /health
    envVars:
      - key: FROST_CLIENT_ID
        sync: false
      - key: RESEND_API_KEY
        sync: false
